# Что изменилось в обучении GNN и почему качество выросло

## Контекст

Изначально `GNN` в `notebooks/gnn_retrieval_neo4j.ipynb` проигрывал `baseline`, который ранжировал сущности напрямую по исходным Qdrant-векторам. Это означало, что графовая модель не добавляла полезный сигнал, а скорее портила уже сильные dense-эмбеддинги.

Проблема была не в самой идее графового обучения, а в постановке:

- модель слишком агрессивно трансформировала хорошие входные векторы;
- использовалось сжатие `256 -> 128`, которое могло терять важную семантику;
- два слоя `GraphSAGE` с `mean`-агрегацией могли переусреднять признаки;
- обучение было недостаточно retrieval-oriented;
- не было механизма сохранить силу baseline и лишь аккуратно добавить графовый сигнал.

## Что именно было изменено

### 1. Добавлен residual fusion поверх исходных векторов

Главное изменение: теперь модель не заменяет исходные Qdrant-векторы графовыми эмбеддингами, а смешивает их.

Идея такая:

- `raw`-эмбеддинги содержат сильную исходную семантику;
- `graph`-эмбеддинги содержат структурный сигнал из `MENTIONS` и `RELATED_TO`;
- итоговое представление получается как нормализованная смесь этих двух источников.

Практически это даёт модели возможность не ломать baseline. Если граф помогает, он вносит поправку. Если не помогает, модель остаётся близко к исходному embedding space.

### 2. Снято лишнее сжатие размерности

Раньше модель делала переход `256 -> 128`. После изменений выходная размерность выровнена с исходной: `256 -> 256`.

Это важно, потому что retrieval по dense-векторам очень чувствителен к тонким семантическим различиям. Если сильно сжимать уже качественные эмбеддинги, модель может терять именно ту информацию, которая делала baseline сильным.

### 3. Добавлена возможность абляций по глубине

Вместо одного фиксированного варианта теперь сравниваются несколько конфигураций:

- `residual_two_layer_ce`
- `residual_one_layer_ce`
- `residual_one_layer_rankloss`

Это позволило не угадывать архитектуру, а проверить, что реально помогает.

Фактически лучший результат дал `1` слой, а не `2`. Это сильный сигнал, что в исходной версии модель страдала от over-smoothing: графовое сообщение было полезным, но слишком глубокий проход начинал размывать различия между узлами.

### 4. Усилен negative sampling

В исходной версии использовались более слабые негативы. После изменений:

- увеличен `hard_negative_top_k`;
- расширен пул hard negatives;
- в лучшей конфигурации используется `6 random + 6 hard` negatives.

Это важно для retrieval, потому что модель должна учиться отличать правильную сущность не от случайного мусора, а от похожих сущностей, которые реально конкурируют в топе.

### 5. Добавлен ranking-oriented loss

Вместо опоры только на `cross_entropy` теперь появилась абляция с pairwise ranking loss через `softplus`.

Это лучше соответствует retrieval-постановке:

- задача не просто “угадай правильный класс”;
- задача “сделай score правильной сущности выше score неправильных”.

Такой loss обычно лучше работает на `Recall@K` и `MRR`, потому что напрямую обучает относительный порядок кандидатов.

### 6. Добавлен preserve loss

Кроме ranking loss, появился мягкий штраф за слишком сильный уход от исходных raw-эмбеддингов.

Это стабилизирует обучение:

- модель не уходит в произвольное новое embedding space;
- сохраняет полезную структуру baseline;
- учится только там, где граф действительно помогает.

### 7. Добавлена расширенная диагностика

Теперь ноутбук показывает не только итоговые метрики, но и:

- абсолютные и относительные delta vs baseline;
- `pos_mean` и `neg_mean`;
- `score_gap`;
- `median_rank`;
- `p90_rank`.

Эта диагностика нужна, чтобы видеть не просто “лучше/хуже”, а понимать, как именно меняется ранжирование.

## Что изменилось в качестве

Лучший вариант оказался `residual_one_layer_rankloss`.

На проверочном запуске он дал:

- `val Recall@5 = 0.1920` против baseline `0.1640`
- `val Recall@10 = 0.2800` против baseline `0.2640`
- `val Recall@20 = 0.3880` против baseline `0.3640`
- `val MRR = 0.1453` против baseline `0.1231`

- `test Recall@5 = 0.1825` против baseline `0.1548`
- `test Recall@10 = 0.2738` против baseline `0.2381`
- `test Recall@20 = 0.3889` против baseline `0.3175`
- `test MRR = 0.1311` против baseline `0.1066`

Если сравнивать с исходным старым GNN, прирост ещё заметнее:

- вырос `Recall@5`;
- вырос `Recall@10`;
- особенно сильно вырос `Recall@20`;
- заметно вырос `MRR`.

Это значит, что модель стала не только чаще находить правильную сущность в топе, но и поднимать её выше в ранжировании.

## Почему прирост оказался настолько большим

### 1. Раньше GNN портил сильный baseline

Главная причина большого прироста в том, что старая версия заставляла GNN “переучивать” уже хорошие эмбеддинги. Это приводило к деградации.

После изменений модель перестала заменять baseline и стала его дополнять.

То есть рост произошёл не потому, что граф внезапно стал магически очень сильным, а потому что:

- убрали главный источник деградации;
- сохранили уже сильный сигнал;
- добавили только ту часть графовой информации, которая действительно полезна.

### 2. Residual fusion дал модели правильную роль

Лучшие обученные residual weights оказались около `0.90` для `chunk` и `entity`.

Это значит:

- около `90%` представления модель хочет оставить из исходных векторов;
- около `10%` хочет добавить из графа.

Это очень важный вывод. Граф здесь полезен не как замена dense retrieval, а как точечная структурная коррекция.

### 3. Один слой оказался лучше двух

Это подтверждает гипотезу об over-smoothing.

На этой задаче, видимо, достаточно одного шага message passing:

- получить локальный контекст по соседям;
- не успеть размыть различия между сущностями.

Второй слой добавлял уже меньше пользы, чем шума.

### 4. Loss стал лучше совпадать с задачей

Когда retrieval обучается через pairwise ranking loss и более сложные negatives, модель начинает оптимизировать именно то, что потом проверяется метриками `Recall@K` и `MRR`.

Из-за этого оптимизация стала существенно более согласованной с целевой задачей.

### 5. Диагностика показала реальное улучшение separation

По диагностическим метрикам у лучшего варианта:

- вырос разрыв между positive и negative similarity;
- улучшился `median_rank`;
- улучшился `p90_rank`.

Это особенно важно: улучшение не только “в среднем”, но и в хвосте распределения. То есть модель стала лучше вытаскивать правильную сущность даже в сложных случаях.

## Главный вывод

Самый важный architectural insight такой:

`GNN` дал большой прирост не потому, что начал полностью заменять baseline, а потому что перестал его ломать.

Итоговая лучшая конфигурация работает как:

- сильная семантическая основа из raw Qdrant-векторов;
- один умеренный графовый шаг;
- retrieval-aware loss;
- более жёсткие negatives;
- мягкая регуляризация, не дающая разрушить исходное пространство.

Именно сочетание этих факторов и дало заметный скачок качества.

## Практическая интерпретация

Если описывать результат максимально прикладно:

- baseline уже хорошо понимает смысл текста;
- граф добавляет структурный контекст;
- новая версия модели научилась использовать граф как “доусилитель”, а не как “замену” dense retrieval;
- поэтому итоговое ранжирование стало лучше, чем и baseline, и старый GNN.

## Обновление: multi-seed план (12 запусков)

Ниже зафиксированы результаты полного плана абляций с протоколом `3 seeds` (`42`, `52`, `62`) и отбором по `mean(val MRR)` + фильтром устойчивости (`std <= 0.01`).

Источник сводки: `notebooks/gnn_training_plan_results.json`.

### Победитель по плану

- `best_from_phase1_or2_plus_cosine_clip`
- `selected_seed = 52` (лучший single-run внутри победившей конфигурации)
- `selected_model_version = graphsage_best_from_phase1_or2_plus_cosine_clip_multi_seed`

Метрики выбранного ранa:

- `val`: `Recall@5=0.1781`, `Recall@10=0.2621`, `Recall@20=0.3639`, `MRR=0.1284`
- `test`: `Recall@5=0.1848`, `Recall@10=0.2532`, `Recall@20=0.3190`, `MRR=0.1168`

### Агрегированная таблица (mean/std по 3 сидам)

Топ-конфигурации по `mean(val MRR)`:

1. `best_from_phase1_or2_plus_cosine_clip`: `val_MRR=0.1274±0.0010`, `test_MRR=0.1181±0.0016`
2. `ce_r8_h8`: `val_MRR=0.1272±0.0011`, `test_MRR=0.1177±0.0034`
3. `rank_r6_h6_m0.10`: `val_MRR=0.1269±0.0011`, `test_MRR=0.1156±0.0013`
4. `ce_r6_h4`: `val_MRR=0.1266±0.0005`, `test_MRR=0.1151±0.0023`
5. `rank_r8_h8_m0.15`: `val_MRR=0.1266±0.0013`, `test_MRR=0.1154±0.0011`

### Что сработало, а что нет

- Сработало: увеличение объёма negative sampling (`ce_r8_h8`) и cosine schedule + clip.
- Сработало умеренно: pairwise rank losses (`rank_r6_h6_m0.10`, `rank_r8_h8_m0.15`) близки к лучшим, но не обогнали фазу 3.
- Не сработало: `EMA` в данной постановке (`best_from_11_plus_ema`) резко просел (`val_MRR ~0.1106`).
- Не сработало: `alpha=0.65` и особенно `2` слоя со stronger residual (наихудшая устойчивость и качество).

### Практический вывод после плана

На текущем срезе данных лучшая стратегия — оставаться в однослойной retrieval-ориентированной постановке с более сильными негативами и аккуратной оптимизацией (`cosine + grad_clip`). Это даёт стабильный прирост к baseline и к исходному `ce_r4_h2` при низком std между seed-ами.

## Операционная оптимизация процесса обучения

Чтобы ускорить повседневные перезапуски, основной training cell в `gnn_retrieval_neo4j.ipynb` переведён на режим по умолчанию `TRAINING_MODE='optimized'`.

Что это меняет:

- по умолчанию запускается только победившая конфигурация (`best_from_phase1_or2_plus_cosine_clip`);
- используется фиксированный production seed (`PROD_SEED=52`);
- сохранены ключевые улучшения плана (`num_random_neg=8`, `num_hard_neg=8`, `cosine` scheduler, `grad_clip=1.0`);
- full-абляции остаются доступны через `TRAINING_MODE='ablation'`.

Дополнительно в training loop включён `smart_hard_negatives`:

- каждые `N` эпох (по умолчанию `3`) модель майнит top ложноположительные `entity` для каждого `chunk`;
- mined-кандидаты объединяются с базовым hard-negative pool;
- итоговый негативный батч становится ближе к реальным ошибкам ранжирования, что обычно полезно для `Recall@10`.

Итог: цикл обучения стал заметно быстрее в ежедневной работе, при этом качество соответствует лучшей найденной конфигурации из эксперимента.

## Этапный аудит качества entity и soft-pruning кандидатов

Ниже зафиксирован отдельный быстрый аудит после роста графа и включения `smart_hard_negatives`.

### Этап 1. Снимок структуры графа

По состоянию на запуск аудита:

- `chunks = 821`
- `entities = 2895`
- `mentions = 4745`
- `related_to = 3048`

Диагностика связности показала высокий хвост слабо связанных сущностей:

- `single_mention_entities = 2149` (доля `~74.2%`)
- quartiles по degree (`mentions`): `1 / 1 / 2`
- quartiles по degree (`related`): `1 / 1 / 2`

Вывод этапа: узкое место — не столько дубли, сколько большой пул "одноразовых" entity.

### Этап 2. Реализация soft-pruning аудита в ноутбуке

В `gnn_retrieval_neo4j.ipynb` добавлена A/B-оценка с маской кандидатов:

- правило soft-pruning: `mentions <= 1 AND related <= 1`;
- метрики считаются в двух режимах:
  - обычный candidate set;
  - pruned candidate set (без физического удаления узлов из Neo4j).

### Этап 3. Результаты A/B (seed=52)

Без pruning:

- baseline test: `Recall@10=0.2063`, `MRR=0.0997`
- selected GNN test: `Recall@10=0.2611`, `MRR=0.1165`

С pruning-кандидатов (`pruned_entities=1766`, `pruned_share=0.61`):

- baseline test (pruned): `Recall@10=0.2189`, `MRR=0.1071`
- selected GNN test (pruned): `Recall@10=0.2589`, `MRR=0.1301`

Интерпретация:

- pruning даёт рост `MRR` (ранг правильной entity выше), особенно у GNN;
- `Recall@10` для GNN почти не меняется (слегка ниже), что ожидаемо при агрессивном фильтре `61%` кандидатов;
- для baseline pruning также улучшает `Recall@10/MRR`, что подтверждает гипотезу о шуме в candidate set.

### Этап 4. Практическое решение

- Пока не удаляем узлы физически.
- Используем soft-pruning как runtime/оценочный фильтр кандидатов.
- Дальше калибруем порог (например, `mentions<=1 & related==0` вместо текущего более жёсткого правила), чтобы сохранить/увеличить `Recall@10` и не потерять выигрыш в `MRR`.

### Этап 5. A/B со смягчённым правилом (`mentions<=1 & related==0`)

Проведён дополнительный контрольный прогон после реализации второго режима pruning в ноутбуке (тот же `seed=52`):

- без pruning:  
  - baseline test: `Recall@10=0.1975`, `MRR=0.1015`  
  - selected GNN test: `Recall@10=0.2458`, `MRR=0.1127`
- strict pruning (`mentions<=1 & related<=1`, `pruned_entities=1766`, `61.0%`):  
  - baseline test: `Recall@10=0.2080`, `MRR=0.1080`  
  - selected GNN test: `Recall@10=0.2437`, `MRR=0.1265`
- soft pruning (`mentions<=1 & related==0`, `pruned_entities=40`, `1.38%`):  
  - baseline test: `Recall@10=0.1954`, `MRR=0.1008`  
  - selected GNN test: `Recall@10=0.2458`, `MRR=0.1125`

Вывод этапа:

- смягчённое правило почти не влияет на метрики (эффект близок к нулю, потому что отсекается слишком мало кандидатов);
- жёсткое правило остаётся лучшим по `MRR`, при умеренном компромиссе по `Recall@10`;
- как рабочий режим для production-подбора кандидатов логично держать strict-версию как опцию re-rank profile, а soft-версию использовать только как «безопасный» фильтр с минимальным влиянием.

### Этап 6. Доп. калибровка порога (`strict` vs `medium` vs `soft`)

Добавлен третий режим A/B в ноутбуке:

- `strict`: `mentions<=1 & related<=1`
- `medium`: `mentions<=1 & related<=2`
- `soft`: `mentions<=1 & related==0`

Результаты на test (тот же `seed=52`, свежий прогон):

- без pruning:
  - baseline: `Recall@10=0.1921`, `MRR=0.0950`
  - selected GNN: `Recall@10=0.2296`, `MRR=0.1070`
- `strict` (`pruned_entities=1778`, `60.97%`):
  - baseline: `Recall@10=0.2004`, `MRR=0.1010`
  - selected GNN: `Recall@10=0.2150`, `MRR=0.1136`
- `medium` (`pruned_entities=2054`, `70.44%`):
  - baseline: `Recall@10=0.2025`, `MRR=0.0928`
  - selected GNN: `Recall@10=0.2109`, `MRR=0.1095`
- `soft` (`pruned_entities=40`, `1.37%`):
  - baseline: `Recall@10=0.1900`, `MRR=0.0943`
  - selected GNN: `Recall@10=0.2296`, `MRR=0.1067`

Вывод этапа:

- `medium` оказался слишком агрессивным (ещё сильнее `strict`) и не даёт лучшего баланса;
- `soft` почти эквивалентен режиму без pruning;
- для режима «максимизировать MRR» остаётся предпочтительным `strict`, для режима «сохранить Recall@10`» — `soft`/без pruning.

## Этап 7. Phase-2 план под цель `Recall@20 >= 0.40` (минимальные изменения)

Реализован отдельный `recall_hunt`-режим в `gnn_retrieval_neo4j.ipynb` с минимальным вмешательством в пайплайн:

- зафиксирован протокол split: `SPLIT_SEED=52`;
- зафиксированы сиды обучения: `PHASE2_SEEDS=[42, 52, 62]`;
- добавлены guardrails отбора:
  - `recall_min_improvement=0.005` (к control),
  - `mrr_drop_guardrail=0.005`,
  - `val_r20_std_threshold=0.02`;
- добавлены диагностики покрытия кандидатов:
  - `oracle_recall@20` по текущим эмбеддингам,
  - `hard_pool_hit_rate@20` по статическому hard-pool.

### Диагностика покрытия (до обучения)

- `oracle_recall@20_val = 0.2973`
- `oracle_recall@20_test = 0.2712`
- `hard_pool_hit_rate@20_val = 0.5251`
- `hard_pool_hit_rate@20_test = 0.5038`

Комментарий: pool покрывает positives лишь примерно в половине случаев, это подтверждает bottleneck по candidate coverage.

### Sweep 4 конфигов × 3 seeds (Recall-focused)

Конфиги:

- `cfgA_control_tail_boost` (control);
- `cfgB_coverage_plus`;
- `cfgC_coverage_plus_plus`;
- `cfgD_stability`.

Агрегированные результаты (`mean/std`):

- `cfgD_stability`: `val_R20=0.3610±0.0019`, `test_R20=0.3513±0.0011`, `val_MRR=0.1292`, `test_MRR=0.1070`
- `cfgB_coverage_plus`: `val_R20=0.3604±0.0022`, `test_R20=0.3532±0.0029`, `val_MRR=0.1302`, `test_MRR=0.1085`
- `cfgA_control_tail_boost`: `val_R20=0.3597±0.0029`, `test_R20=0.3506±0.0029`, `val_MRR=0.1296`, `test_MRR=0.1087`
- `cfgC_coverage_plus_plus`: `val_R20=0.3597±0.0029`, `test_R20=0.3487±0.0040`, `val_MRR=0.1287`, `test_MRR=0.1069`

### Отбор по guardrails и финальный выбор

По заданным guardrails ни один кандидат не дал достаточного прироста к control по `test_R20` (требование `+0.005`), поэтому автоматически выбран control:

- `selected_experiment = cfgA_control_tail_boost` (`seed=52` как лучший внутри конфига по val);
- финальные метрики:
  - `val`: `Recall@20=0.3629`, `Recall@10=0.2568`, `MRR=0.1320`
  - `test`: `Recall@20=0.3500`, `Recall@10=0.2346`, `MRR=0.1089`

Итог этапа:

- цель `Recall@20 >= 0.40` на текущем объёме/качестве candidate set не достигнута;
- минимальные тюнинги модели/negative mining дали устойчивую зону `test Recall@20 ~0.35`;
- дальнейший шаг для прорыва к `0.40` — не «докручивать loss», а расширять и улучшать candidate coverage (данные/entity quality/двухстадийный retrieval).
